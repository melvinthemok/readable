<div class="jumbotron" style="background-color:#d9edf7">
  <div class="container">
    <div class="row">
      <div class="col-sm-6 offset-sm-3">
        <h2 class="display-4">attendance</h2><br>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-sm-6 offset-sm-3">
      <form class="mt-3 mb-5" method="POST" action=<%= "/attend/" + currentUser._id + "?_method=PUT" %>>
        <label for="attending">Coming this Saturday?</label><br>
          <select class="custom-select" id="attendingInput" name="attending">
            <option selected disabled hidden>Please select</option>
            <option value=true>Yes</option>
            <option value=false>No</option>
          </select>
        <button class="btn btn-primary" type="submit">Submit</button>
      </form>
      <!--<form class="mt-3 mb-5" method="POST" action=<%= "/attend/" + currentUser._id + "?_method=PUT" %>>
        <div class="mb-5">Coming this Saturday?</div>
        <div class="d-flex justify-content-around">
          <div>
            <button class="btn btn-success" type="submit" name=attending value=true>Yes</button>
          </div>
          <div>
            <button class="btn btn-danger" type="submit" name=attending value=false>No</button>
          </div>
        </div>
      </form>-->
    </div>
  </div>
</div>