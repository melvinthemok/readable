<div class="container">
  <div class="row">
    <div class="col-sm-8 offset-sm-2">
      <nav class="navbar navbar-default navbar-toggleable-xl navbar-light">
        <a class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <% if (currentUser) { %>
        <p>hi, <%= currentUserName %>!</p>
        <% } %>
        <% if (!currentUser) { %>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
          <span>☰</span>
        </button>
        <% } %>
        </a>
        <a class="nav-link" href="/">Home</a>
        <div class="collapse navbar-collapse ml-auto" id="navbarSupportedContent">
          <div class="navbar-nav ml-auto">
          <%
          var loggedInStyle = ""
          var loggedOutStyle = ""
          var currentUserName = ""
          var isStudentStyle= ""
          if (currentUser) {
            loggedInStyle = "display:none; visibility:hidden;"
            currentUserName = currentUser.name
            currentUserId = currentUser.id
            if (currentUser.role === "student") {
              isStudentStyle = "display:none; visibility hidden;"
            }
          } else if (!currentUser) {
            loggedOutStyle = "display:none; visibility:hidden;"
            currentUserName = ""
            currentUserId = ""
          }
          %>
            <a class="nav-item nav-link" href="/auth/signup" style="<%= loggedInStyle %>">sign up </a>
            <a class="nav-item nav-link" href="/auth/login" style="<%= loggedInStyle %>">log in</a>
            <div class="dropdown">
              <a class="nav-item nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" style="<%= loggedOutStyle %>">hey, <%= currentUserName %>!</a>
              <div class="dropdown-menu dropdown-menu-right" style="<%= loggedOutStyle %> position: absolute; z-index: 1;">
                <!--<h6 class="dropdown-header" style="<%= loggedOutStyle %>"><em>let us know if you are</em></h6>
                <a class="dropdown-item" href="/attend" style="<%= loggedOutStyle %>">attending</a>
                <h6 class="dropdown-header" style="<%= loggedOutStyle %> <%= isStudentStyle %>"><em>view this Saturday's</em></h6>
                <a class="dropdown-item" href="/group" style="<%= loggedOutStyle %> <%= isStudentStyle %>">groups</a>-->
                <h6 class="dropdown-header"><em>awesome</em></h6>
                <a class="dropdown-item" href="/student/new">sign up a student</a>
                <div class="dropdown-divider"></div>
                <h6 class="dropdown-header"><em>stay don't go</em></h6>
                <a class="dropdown-item" href="/auth/logout">log out</a>
              </div>
            </div>
          </div>
        </div>
      </nav>
    </div>
  </div>
</div>